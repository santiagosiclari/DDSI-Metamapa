/*
package domain.business.Usuarios;


import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;
import java.util.List;

class UsuarioTest {

  @Test
  void cuandoUsuarioTieneRolVisualizador_debeReconocerlo() {
    Perfil perfil = new Perfil("Emi", "Siclari", 25);
    Usuario u = new Usuario("emi@ejemplo.com", "clave", perfil, List.of(Rol.VISUALIZADOR));
    assertTrue(u.tieneRol(Rol.VISUALIZADOR));
  }

  @Test
  void cuandoUsuarioNoTieneRolContribuyente_debeRechazar() {
    Perfil perfil = new Perfil("Test", "User", 30);
    Usuario u = new Usuario("test@ejemplo.com", "clave", perfil, List.of(Rol.ADMINISTRADOR));
    assertFalse(u.tieneRol(Rol.CONTRIBUYENTE));
  }
}
*/
